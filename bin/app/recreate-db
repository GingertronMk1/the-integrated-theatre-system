#!/usr/bin/env sh

source .env

./bin/app/console doctrine:database:drop --force \
    && ./bin/app/console doctrine:database:create \
    && ./bin/app/console doctrine:migrations:migrate -n \
    && ./bin/app/console app:load-fixtures
