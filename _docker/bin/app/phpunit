#!/usr/bin/env sh

echo 'APP_ENV=test' > .env.local
echo

./_docker/bin/app/migrate -- -e test

./_docker/bin/app/composer phpunit -- --coverage-text

./_docker/bin/app/composer phinx -- rollback -e test -t 0

rm .env.local
